#!/bin/bash

MPD_DIR="$HOME/.mpd/"
MPC="$(which mpc) -q"
MPD="$(which mpd) $MPD_DIR/mpd.conf"

if [ -f "$MPD_DIR/pid" ]; then
  # MPD running
  $MPC toggle
else
  $MPD
  sleep 1 # Sleep a second to let MPD start
  $MPC play
fi

