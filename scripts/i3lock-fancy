#!/usr/bin/env bash

# Dependencies:
# imagemagick
# i3lock-color-git
# scrot

image=/tmp/i3lock.png
lock="$HOME/.local/share/i3lock/lock.png"

scrot "$image"
convert "$image" -scale 5% -scale 2000% - | composite -gravity center "$lock" - "$image"
i3lock -i "$image"
rm "$image"
