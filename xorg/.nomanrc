#!/usr/bin/bash

read -r -d '' applications <<'EOF'
kodi
EOF

application="$(echo "$applications" | fzf-urxvt)"

if [[ "$application" == "kodi" ]]; then
  exec kodi
else
  echo "No application selected."
  exit 0
fi
