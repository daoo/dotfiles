#!/bin/zsh

# Skip all this for non-interactive shells
[[ -z "$PS1" ]] && return

. ~/dotfiles/zsh/config
. ~/dotfiles/zsh/aliases
. ~/dotfiles/zsh/binds
. ~/dotfiles/zsh/autocompletion
. ~/dotfiles/zsh/functions

. ~/dotfiles/zsh/plugins/z/z.sh
function chpwd() {
  z --add "$(pwd -P)"
}

