pkgname=powersave
pkgver=3
pkgrel=1
pkgdesc="Custom program for changing power save state."
url="http://github.com/daoo/dotfiles/powersave"
arch=('x86_64' 'i686')
depends=()
makedepends=('clang' 'make' 'xz')
source=("powersave.tar.xz")

build() {
  cd "${srcdir}/"
  make release cxx=g++
}

package() {
  cd "${srcdir}/"
  install -Dm 755 "build/$pkgname" "$pkgdir/usr/bin/$pkgname"
}

# vim:set ts=2 sw=2 et:
