snippet module "module" b
module `!p
if path and os.path.exists(path):
	(base, ext) = os.path.splitext(os.path.abspath(path))
	tmp         = base.split("/")
	module      = []
	while tmp:
		s = tmp.pop()
		if s[0] in "ABCDEFGHIJKLMNOPQRSTUVWXYZ":
			module.insert(0, s)
		else:
			break

	snip.rv = ".".join(module)
else:
	snip.rv = snip.basename` where
endsnippet
